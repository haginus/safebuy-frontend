import { StyleSheet } from "react-native"

export default StyleSheet.create({
  section: {
    padding: 16,
    marginHorizontal: 12,
    borderRadius: 8,
  },
  sectionHeader: {
    fontWeight: "500",
    marginBottom: 8,
  },
})